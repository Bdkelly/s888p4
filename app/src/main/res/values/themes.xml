<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Project4" parent="android:Theme.Material.Light.NoActionBar" />

        <!-- Splash Screen Theme with No Action Bar -->
        <style name="Theme.Project4.NoActionBar" parent="Theme.Project4">
            <item name="windowActionBar">false</item>
            <item name="windowNoTitle">true</item>
        </style>

    <style name="Theme.Project4.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Project4.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="Project4.NoActionBar" parent="Theme.MaterialComponents.DayNight">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>


        </resources>